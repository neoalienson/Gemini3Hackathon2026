# Web framework (FastAPI/Starlette >=0.115 use anyio 4.x; required by google-genai)
fastapi>=0.115.0
uvicorn[standard]>=0.24.0

# Database & ORM
sqlalchemy==2.0.23
pymysql==1.1.0
cryptography==41.0.7
psycopg2-binary==2.9.9
cloud-sql-python-connector[pymysql]

# Config & validation (pydantic 2.7+ for FastAPI 0.128)
python-dotenv==1.0.0
pydantic>=2.5.0
pydantic-settings>=2.0.0

# OpenTelemetry (pinned so google-adk and GCP exporters agree on SDK version)
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0

# Google Cloud & Vertex AI
google-auth>=2.0.0
google-api-core==2.19.2
# 3.13.x pins protobuf<5; opentelemetry-proto 1.37 needs protobuf>=5. Use 3.30+ for protobuf 5.x.
google-cloud-bigquery==3.30.0
# Pin ADK transitive deps to exact versions so pip doesn't backtrack through dozens
google-cloud-aiplatform[agent-engines]==1.135.0
google-cloud-pubsub==2.34.0
google-cloud-spanner==3.62.0

# Vertex AI: Google Gen AI SDK (no LangChain)
# Pin minimum so pip cannot resolve to 1.4.x (missing APIs)
google-genai==1.60.0

# Google Agent Development Kit (provides google.adk namespace)
# Pin minimum so pip cannot resolve to 0.0.1 (no google.adk module)
google-adk==1.23.0

# Data processing for consolidation step
pandas>=2.0.0
